<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- Dependencies (share/file-picker/audio plugins) pull in media & storage
         read permissions by default. RouteShare never reads the user's photos,
         videos, or music: file import uses the system document picker (SAF) and
         the chime plays from a bundled asset. Remove them so Play does not flag
         the app for the Photo & Video permissions declaration. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" tools:node="remove" />

    <application
        android:label="RouteShare"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <!-- Google Maps SDK for Android API key.
             Replace the value with your own key from Google Cloud Console
             (enable "Maps SDK for Android" and restrict the key to this app's
             package name + signing SHA-1). Map DISPLAY is free/unlimited; a
             billing account must still be attached to issue the key. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Open a shared route file: tap a route in WhatsApp, Drive, a file
                 manager, etc. and offer to import it into RouteShare. Route files
                 are JSON. We match a few ways because different senders tag .json
                 with different MIME types. -->
            <!-- 1. JSON MIME types -->
            <intent-filter android:label="Import into RouteShare">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="content" android:mimeType="application/json"/>
                <data android:scheme="file" android:mimeType="application/json"/>
                <data android:scheme="content" android:mimeType="text/json"/>
                <data android:scheme="file" android:mimeType="text/json"/>
            </intent-filter>
            <!-- 2. Any file whose name ends in .json (covers generic MIME from
                 file managers where the URI keeps the filename) -->
            <intent-filter android:label="Import into RouteShare">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="content" android:host="*"
                      android:mimeType="*/*" android:pathPattern=".*\\.json"/>
                <data android:scheme="file" android:host="*"
                      android:mimeType="*/*" android:pathPattern=".*\\.json"/>
            </intent-filter>
            <!-- 3. Shared via the system share sheet -->
            <intent-filter android:label="Import into RouteShare">
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="application/json"/>
                <data android:mimeType="text/json"/>
            </intent-filter>
        </activity>

        <!-- flutter_foreground_task: keeps app in foreground -->
        <service
            android:name="com.pravera.flutter_foreground_task.service.ForegroundTaskService"
            android:foregroundServiceType="location"
            android:exported="false" />

        <!-- geolocator: keeps the location stream alive when screen is off -->
        <service
            android:name="com.baseflow.geolocator.GeolocatorLocationService"
            android:foregroundServiceType="location"
            android:exported="false" />

        <!-- flutter_overlay_window: the floating guided-drive overlay service -->
        <service
            android:name="flutter.overlay.window.flutter_overlay_window.OverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Shows the next guided-drive stop in a floating window over the map" />
        </service>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <package android:name="com.google.android.apps.maps"/>
        <!-- flutter_tts: discover the text-to-speech engine (Android 11+) -->
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
    </queries>
</manifest>
